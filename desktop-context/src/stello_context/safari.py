"""Safari tab introspection via AppleScript + hostname-suffix blocklist.

Filled in at step 9. Tabs are CONTEXT only — never written to items table,
never indexed, never returned in /related results. Blocked tabs are counted
but their URL/title are never embedded, logged, or persisted.
"""
