"""Structured logging helpers.

For now, daemon.py configures the root logger directly. This module exists
as a placeholder for richer structured-logging helpers (e.g. JSON formatter,
per-module log levels) added as the daemon grows.
"""
