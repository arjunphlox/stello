"""HTTP client for the MLX server at 127.0.0.1:8765.

Filled in at step 3. Mirrors the stello_mlx.Client API surface
(embed, generate, vision, generate_structured, vision_structured).
Intentionally vendored — not importing from the MLX Stack repo —
so this package has no PYTHONPATH coupling to a sibling project.
"""
