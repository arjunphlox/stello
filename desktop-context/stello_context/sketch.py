"""Sketch introspection — ZIP parsing, AppleScript glue, visible-rect math.

Filled in across:
  - step 7  ZIP parse (strings, artboard names, page metadata)
  - step 10 open-document discovery + visible-rect intersection
  - step 11 AppleScript-driven artboard export + dwell-gated VLM
"""
